# Velocity — <delivery>

<!-- One line per COMPLETED sprint: the points that met the Definition of Done.
     Written at retro, never at planning. A sprint that shipped nothing still
     gets a line (0) — the gap is the signal. -->

- S01: <points>

Rolling average (<n> sprint(s)): <avg>. Wall-clock: <how long the sprint actually took, and
anything that distorted it>. Velocity is a planning signal for this crew only — never a target,
never compared across deliveries, never used as a productivity measure.
